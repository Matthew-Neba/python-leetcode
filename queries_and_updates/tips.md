-   No updates, range queries - prefix arrays / sparse tables

-   Point updates, rare range queries (up to a constant) - difference arrays (if operation allows) / segment trees

-   Point updates, range queries - segment trees

-   Range updates, range queries - lazy propagation segment trees
